#include "main.h"
#include <stdio.h>

/**
 * _strlen_recursion - Entry point
 *
 * @s: input string
 *
 * Return: 0 always
 */

int _strlen_recursion(char *s)
{
	if (*s != '\0')
	{
		printf("%d", strlen(s);
	}
	return 0;
}
