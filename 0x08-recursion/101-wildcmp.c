#include "main.h"
#include <string.h>

/**
 * wildcmp - entry
 * @s1: ist char
 * @s2: 2nd char
 * Return: 0
 */

int wildcmp(char *s1, char *s2)
{
	if (wildcmp(s1, s2))
		return 1;
	else
		return 0;
}
