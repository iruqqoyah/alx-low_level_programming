#include "main.h"

/**
 * _strcat - strch
 * @dest: destination char
 * @sre: source strinf
 * Return: 0 if no error
 */


char *_strcat(char *dest, char *src)
{
int count = 0, count2 = 0;



	while (*(dest + count) != '\0')

	{

		countwhile (count2 >= 0)

	{

		*(dest + count) = *(src + c		if (*(src + count2) == '\0')

			break;

		count++;

		count2++;

	}

	return (dest);

}	
